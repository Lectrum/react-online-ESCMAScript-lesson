const greeting = 'Добро пожаловать,';
const user = 'Vova';

/**
 * Это — именованный экспорт (named export).
 * В отличии от экспорта по-умолчанию, в инструкции именованного экспорта
 * сущности нужно заключать в фигурные скобки.
 * В одном файле может быть сколько угодно named export'ов.
 */
export { greeting, user };
